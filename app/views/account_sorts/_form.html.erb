<%= form_with(model: account_sort, local: true) do |form| %>
  <% if account_sort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_sort.errors.count, "error") %> prohibited this account_sort from being saved:</h2>

      <ul>
      <% account_sort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sort %>
    <%= form.text_field :sort %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
