<%= form_with(model: account_history, local: true) do |form| %>
  <% if account_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_history.errors.count, "error") %> prohibited this account_history from being saved:</h2>

      <ul>
      <% account_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.number_field :account_id %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
