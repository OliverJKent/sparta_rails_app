<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_num %>
    <%= form.number_field :account_num %>
  </div>

  <div>
    <%= form.label :account_sort %>
  <%= collection_select(:accounts, :account_sort, AccountSort.all, :id, :sort, {:prompt => false}) %>
</div>

  <!-- <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div> -->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
